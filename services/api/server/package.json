{
  "name": "dataforge-analytics-api",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsup src/handler.ts --format cjs --sourcemap --minify --dts false --out-dir dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts",
    "deploy": "npm run build"
  },
  "dependencies": {
    "@aws-sdk/client-athena": "^3.470.0",
    "@aws-sdk/client-dynamodb": "^3.470.0",
    "@aws-sdk/lib-dynamodb": "^3.470.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.122",
    "@types/node": "^20.11.17",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.29.1",
    "tsup": "^7.2.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2"
  }
}
